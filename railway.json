{
  "build": {
    "env": {
      "PLAYWRIGHT_BROWSERS_PATH": "0"
    },
    "cmd": "playwright install"
  },
  "start": "uvicorn web_bypass:app --host 0.0.0.0 --port 8080 --proxy-headers"
}
